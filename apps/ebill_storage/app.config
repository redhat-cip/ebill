[
  {ebill_pool, [{type, storage}]},
  {lager, [
    {handlers, [
      {lager_console_backend, info},
      {lager_file_backend, [{file, "log/ebill_storage_error.log"}, {level, error}]},
      {lager_file_backend, [{file, "log/ebill_storage_console.log"}, {level, info}]}
    ]}
  ]}
].

[
  {ebill_pool, [{type, server}]},
  {lager, [
    {handlers, [
      {lager_console_backend, info},
      {lager_file_backend, [{file, "log/ebill_server_error.log"}, {level, error}]},
      {lager_file_backend, [{file, "log/ebill_server_console.log"}, {level, info}]}
    ]}
  ]}
].
